# Three drones, one GCS.
# Stops broadcast telemetry from sibling drones leaking onto another
# drone's uplink.
#
#   ┌── Drone 1 ──┐                       ┌──────── Ground ─────────────┐
#   │             │                       │   ┌──────┐                  │
#   │             │◄──────────────────────┼──►│      │                  │
#   └─────────────┘                       │   │      │                  │
#   ┌── Drone 2 ──┐                       │   │      │                  │
#   │             │                       │   │ RMR  │◄────► GCS        │
#   │             │◄──────────────────────┼──►│      │                  │
#   └─────────────┘                       │   │      │                  │
#   ┌── Drone 3 ──┐                       │   │      │                  │
#   │             │                       │   │      │                  │
#   │             │◄──────────────────────┼──►│      │                  │
#   └─────────────┘                       │   └──────┘                  │
#                                         └─────────────────────────────┘

# Uplink to drone 1.
[endpoint.drone1]
type = "udpc"
host = "192.168.1.11"
port = 14550
allow_src_endpoint_out = "gcs"

# Uplink to drone 2.
[endpoint.drone2]
type = "udpc"
host = "192.168.1.12"
port = 14550
allow_src_endpoint_out = "gcs"

# Uplink to drone 3.
[endpoint.drone3]
type = "udpc"
host = "192.168.1.13"
port = 14550
allow_src_endpoint_out = "gcs"

# GCS over TCP.
[endpoint.gcs]
type = "tcps"
bind = "0.0.0.0:5760"

# Equivalent CLI invocation:
#
# rmr udpc:192.168.1.11:14550#drone1?allow_src_endpoint_out=gcs \
#     udpc:192.168.1.12:14550#drone2?allow_src_endpoint_out=gcs \
#     udpc:192.168.1.13:14550#drone3?allow_src_endpoint_out=gcs \
#     tcps:0.0.0.0:5760#gcs
